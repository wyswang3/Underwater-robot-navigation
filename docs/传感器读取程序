如果你是通过 AppImage 启动的 QGroundControl，确保文件已经赋予了执行权限：
chmod +x QGroundControl.AppImage
./QGroundControl.AppImage
启动仿真（使用 SITL 模式）：
Tools/autotest/sim_vehicle.py -v ArduSub --frame vectored --console --map
--console 让你可以看到命令行的日志。

--map 会显示地图视图，确保你可以看到 ROV 的运动。
仿真默认会使用 UDP 端口 14550 来进行通信，确保 QGC 和仿真使用的是相同的端口。如果有需要，你也可以手动指定端口，例如：
Tools/autotest/sim_vehicle.py -v ArduSub --frame vectored --console --map --mavlink-port 14550
在修复了 c_cpp_properties.json 格式问题后，重新加载 VS Code，并尝试编译：
./waf configure --board sitl
./waf build


PYTHONPATH=. /usr/bin/python3 -u apps/imu_realtime_pipeline.py

查看终端界面

tmux attach -t data_session

DVL测试程序命令行代码:
# 在项目根目录
pip install -e .
# 成功后随处都能 import uwnav
python apps/tools/dvl_data_verifier.py --port COM6 --baud 115200 --send-cs --send-cz --log INFO 

# 进入项目根目录
python apps/tools/dvl_serial_logger.py --port /dev/ttyUSB1 --baud 115200 --log INFO
python .\apps\tools\dvl_data_verifier.py `
  --port COM6 --baud 115200 `
  --df 0 --pr 10 --pm 5 --send-cs --send-cz `
  --log INFO


# Windows 例子： --port COM5
# 指定输出目录（可选）
python apps/tools/dvl_serial_logger.py --outdir ./data
<<<<<<< HEAD
=======


# 单独测试传感器读取程序
# 1. 进入项目根目录
cd ~/ROV/Underwater-robot-navigation-feature-IMU-DVL-serial

# 2. 将当前目录加入 Python 搜索路径 (这一步解决了 "No module named uwnav")
export PYTHONPATH=$PWD

# 3. 进入脚本所在目录
cd apps/acquire
>>>>>>> collaborator_IMU_DVL

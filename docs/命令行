如果你是通过 AppImage 启动的 QGroundControl，确保文件已经赋予了执行权限：
chmod +x QGroundControl.AppImage
./QGroundControl.AppImage
启动仿真（使用 SITL 模式）：
Tools/autotest/sim_vehicle.py -v ArduSub --frame vectored --console --map
--console 让你可以看到命令行的日志。

--map 会显示地图视图，确保你可以看到 ROV 的运动。
仿真默认会使用 UDP 端口 14550 来进行通信，确保 QGC 和仿真使用的是相同的端口。如果有需要，你也可以手动指定端口，例如：
Tools/autotest/sim_vehicle.py -v ArduSub --frame vectored --console --map --mavlink-port 14550
在修复了 c_cpp_properties.json 格式问题后，重新加载 VS Code，并尝试编译：
./waf configure --board sitl
./waf build

ubuntu激活windterm：
(rov_rl_env) wys@wys-System-Product-Name:~/WindTerm_2.7.0$ chmod +x WindTerm
(rov_rl_env) wys@wys-System-Product-Name:~/WindTerm_2.7.0$ ./WindTerm

你用 WindTerm 先 SSH 到鲁班猫，然后在那个终端里“从 Ubuntu 拉取”（Pull）。要求 Ubuntu 上开着 SSH 服务。
sudo apt update
sudo apt install -y openssh-server
在 Ubuntu 上运行：ip link show
你可以用临时命令配置：
sudo ip addr add 192.168.2.2/24 dev enp3s0   # 假设网口是 enp3s0
sudo ip link set enp3s0 up

ubuntu调整网络端口：
（假设鲁班猫是 192.168.2.250，你的 Ubuntu 设置为 192.168.2.2）
sudo ip addr flush dev enp7s0                      # 清除原有 IP，避免冲突
sudo ip addr add 192.168.2.2/24 dev enp7s0         # 配置 Ubuntu 的 IP
sudo ip link set enp7s0 up                         # 确保网口启动

查看配置是否生效
ip addr show dev enp7s0
sudo ip route add 192.168.2.0/24 dev enp7s0

ping -c 4 192.168.2.250

拉取Github远程更新并合并分支:
./git-auto.sh -b feature/IMU-DVL-serial -p merge -m "调整了python配置文件路径"
